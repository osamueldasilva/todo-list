import { styled } from "styled-components";

export const StyleHeader = styled.header`
    display: flex;
    justify-content: center;

    padding: 2rem;
    width: 100%;

    img {
        height: 40px;
        width: 40px;
    }
`